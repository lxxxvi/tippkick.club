%h1= t('shared.dashboard')

.grid.grid-cols-1.gap-8.sm:grid-cols-2
  = render DashboardPredictionsComponent.new(current_user)
  = render DashboardTeamsComponent.new(current_user)
  = render DashboardProfileComponent.new(current_user)
