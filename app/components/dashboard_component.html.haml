%h1= title

.grid.grid-cols-1.gap-10.sm:grid-cols-2
  = render DashboardRankingComponent.new(user)
  = render DashboardBetsComponent.new(user)
  = render DashboardTeamsComponent.new(user)
  = render DashboardProfileComponent.new
